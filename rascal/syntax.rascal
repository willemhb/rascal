# This file is for testing the scanner/parser for the current build.

module Math do
    fun even?(x) do
        if x % 2 == 0 do
            true
        else
            false
        end
    end
end