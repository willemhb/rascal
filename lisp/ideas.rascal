# speculative non-lisp syntax.

# module declaration
provide Base

@doc Base, "Rascal base library."

# example import usage
require Core
require "regex-state-machine.rascal" as RESM

# utilities
fun method_error(values...) do
  val types_string = values |> map(typeof) |> map(Str) |> join(", ")

  raise :method-error, "No method matching (${types_string})"
end

