#ifndef rascal_compile_h
#define rascal_compile_h

#include "types.h"

// forward declarations -------------------------------------------------------
value_t compile( value_t expr );
value_t compile_file( char *fname );

#endif
